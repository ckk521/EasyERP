# 系统管理模块 - 数据库设计

> WMS仓储管理系统 - 系统管理模块数据库 Schema 设计

---

## 一、ER图

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│    用户      │       │    角色      │       │    权限      │
├─────────────┤       ├─────────────┤       ├─────────────┤
│ id          │───┐   │ id          │       │ id          │
│ username    │   │   │ code        │───┐   │ code        │
│ name        │   └──►│ name        │   │   │ name        │
│ password    │       │ type        │   └──►│ module      │
│ phone       │       │ description │       │ description │
│ email       │       │ status      │       └─────────────┘
│ role_id     │       │ is_system   │              │
│ warehouse_id│       │ created_at  │              │
│ status      │       │ updated_at  │              │
│ created_at  │       └─────────────┘              │
│ updated_at  │              │                     │
└─────────────┘              │              ┌──────┴──────┐
                             │              │  角色权限关联 │
                             │              ├─────────────┤
                             │              │ role_id     │
                             └──────────────│ permission_id│
                                           │ created_at  │
┌─────────────┐       ┌─────────────┐      └─────────────┘
│    仓库      │       │  操作日志    │
├─────────────┤       ├─────────────┤
│ id          │       │ id          │
│ code        │       │ time        │
│ name        │       │ user_id     │
│ type        │       │ username    │
│ country     │       │ module      │
│ province    │       │ action      │
│ address     │       │ object      │
│ status      │       │ ip          │
│ created_at  │       │ result      │
└─────────────┘       │ details     │
                      │ created_at  │
┌─────────────┐       └─────────────┘
│  登录日志    │
├─────────────┤
│ id          │
│ time        │
│ user_id     │
│ username    │
│ action      │
│ ip          │
│ device      │
│ result      │
│ fail_reason │
│ created_at  │
└─────────────┘

┌─────────────┐       ┌─────────────┐
│  系统配置    │       │ 配置历史     │
├─────────────┤       ├─────────────┤
│ id          │       │ id          │
│ category    │◄──────│ config_id   │
│ code        │       │ old_value   │
│ name        │       │ new_value   │
│ value       │       │ operator_id │
│ type        │       │ operator    │
│ default_value│      │ reason      │
│ description │       │ created_at  │
│ is_system   │       └─────────────┘
│ created_at  │
│ updated_at  │
└─────────────┘
```

---

## 二、表结构详细设计

### 2.1 用户表 (sys_user)

```sql
CREATE TABLE sys_user (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID',
    username        VARCHAR(50) NOT NULL UNIQUE COMMENT '用户名',
    password        VARCHAR(255) NOT NULL COMMENT '密码（加密）',
    name            VARCHAR(100) NOT NULL COMMENT '姓名',
    phone           VARCHAR(20) COMMENT '手机号',
    email           VARCHAR(100) COMMENT '邮箱',
    role_id         BIGINT NOT NULL COMMENT '角色ID',
    warehouse_id    BIGINT COMMENT '所属仓库ID',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
    last_login_time DATETIME COMMENT '最后登录时间',
    last_login_ip   VARCHAR(50) COMMENT '最后登录IP',
    create_user     BIGINT COMMENT '创建人ID',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_user     BIGINT COMMENT '更新人ID',
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_username (username),
    INDEX idx_role_id (role_id),
    INDEX idx_warehouse_id (warehouse_id),
    INDEX idx_status (status),
    INDEX idx_create_time (create_time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**字段说明**：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键自增 |
| username | VARCHAR(50) | 唯一登录账号 |
| password | VARCHAR(255) | BCrypt加密存储 |
| name | VARCHAR(100) | 真实姓名 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱地址 |
| role_id | BIGINT | 关联角色 |
| warehouse_id | BIGINT | 所属仓库，可为空（系统管理员无仓库） |
| status | TINYINT | 0-禁用，1-启用 |
| last_login_time | DATETIME | 最后登录时间 |
| last_login_ip | VARCHAR(50) | 最后登录IP |
| is_system | TINYINT | 是否系统内置：0-否，1-是 |

---

### 2.2 角色表 (sys_role)

```sql
CREATE TABLE sys_role (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '角色ID',
    code            VARCHAR(50) NOT NULL UNIQUE COMMENT '角色编码',
    name            VARCHAR(100) NOT NULL COMMENT '角色名称',
    type            TINYINT NOT NULL DEFAULT 2 COMMENT '类型：1-预置，2-自定义',
    description     VARCHAR(500) COMMENT '角色描述',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-禁用，1-启用',
    is_system       TINYINT NOT NULL DEFAULT 0 COMMENT '是否系统内置：0-否，1-是',
    create_user     BIGINT COMMENT '创建人ID',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_user     BIGINT COMMENT '更新人ID',
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_code (code),
    INDEX idx_type (type),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';
```

**预置角色数据**：

```sql
INSERT INTO sys_role (code, name, type, description, status, is_system) VALUES
('SUPER_ADMIN', '超级管理员', 1, '拥有系统全部权限', 1, 1),
('WH_ADMIN', '仓库管理员', 1, '本仓库全部业务权限', 1, 1),
('INBOUND_OPERATOR', '入库员', 1, '入库相关功能权限', 1, 1),
('OUTBOUND_OPERATOR', '出库员', 1, '出库相关功能权限', 1, 1),
('INVENTORY_ADMIN', '库存管理员', 1, '库存查询、盘点、调拨权限', 1, 1),
('REPORT_VIEWER', '报表查看员', 1, '仅查看报表权限', 1, 1);
```

---

### 2.3 权限表 (sys_permission)

```sql
CREATE TABLE sys_permission (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '权限ID',
    code            VARCHAR(100) NOT NULL UNIQUE COMMENT '权限编码',
    name            VARCHAR(100) NOT NULL COMMENT '权限名称',
    module          VARCHAR(50) NOT NULL COMMENT '所属模块',
    description     VARCHAR(500) COMMENT '权限描述',
    parent_id       BIGINT COMMENT '父权限ID',
    sort_order      INT DEFAULT 0 COMMENT '排序',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_module (module),
    INDEX idx_parent_id (parent_id),
    INDEX idx_code (code)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限表';
```

**权限数据**：

```sql
-- 模块级权限
INSERT INTO sys_permission (code, name, module, description, parent_id) VALUES
-- 系统管理模块
('system:user:view', '用户查看', '系统管理', '查看用户列表和详情', NULL),
('system:user:create', '用户创建', '系统管理', '创建新用户', NULL),
('system:user:edit', '用户编辑', '系统管理', '编辑用户信息', NULL),
('system:user:delete', '用户删除', '系统管理', '删除用户', NULL),
('system:user:enable', '用户启用', '系统管理', '启用用户', NULL),
('system:user:disable', '用户禁用', '系统管理', '禁用用户', NULL),
('system:user:resetpwd', '重置密码', '系统管理', '重置用户密码', NULL),

('system:role:view', '角色查看', '系统管理', '查看角色列表和详情', NULL),
('system:role:create', '角色创建', '系统管理', '创建新角色', NULL),
('system:role:edit', '角色编辑', '系统管理', '编辑角色信息', NULL),
('system:role:delete', '角色删除', '系统管理', '删除角色', NULL),

('system:config:view', '配置查看', '系统管理', '查看系统配置', NULL),
('system:config:edit', '配置编辑', '系统管理', '编辑系统配置', NULL),
('system:config:export', '配置导出', '系统管理', '导出系统配置', NULL),
('system:config:import', '配置导入', '系统管理', '导入系统配置', NULL),

('system:log:view', '日志查看', '系统管理', '查看操作日志', NULL),
('system:log:export', '日志导出', '系统管理', '导出操作日志', NULL),

-- 入库管理模块
('inbound:view', '入库查看', '入库管理', '查看入库单', NULL),
('inbound:create', '入库创建', '入库管理', '创建入库单', NULL),
('inbound:edit', '入库编辑', '入库管理', '编辑入库单', NULL),
('inbound:delete', '入库删除', '入库管理', '删除入库单', NULL),
('inbound:receive', '收货作业', '入库管理', '执行收货操作', NULL),
('inbound:accept', '验收作业', '入库管理', '执行验收操作', NULL),
('inbound:putaway', '上架作业', '入库管理', '执行上架操作', NULL),

-- 出库管理模块
('outbound:view', '出库查看', '出库管理', '查看出库单', NULL),
('outbound:create', '出库创建', '出库管理', '创建出库单', NULL),
('outbound:edit', '出库编辑', '出库管理', '编辑出库单', NULL),
('outbound:delete', '出库删除', '出库管理', '删除出库单', NULL),
('outbound:wave', '波次分配', '出库管理', '执行波次分配', NULL),
('outbound:pick', '拣货作业', '出库管理', '执行拣货操作', NULL),
('outbound:pack', '打包作业', '出库管理', '执行打包操作', NULL),
('outbound:ship', '发货作业', '出库管理', '执行发货操作', NULL),

-- 库存管理模块
('inventory:view', '库存查看', '库存管理', '查看库存', NULL),
('inventory:stocktake', '库存盘点', '库存管理', '执行盘点', NULL),
('inventory:transfer', '库存调拨', '库存管理', '执行调拨', NULL),
('inventory:freeze', '库存冻结', '库存管理', '冻结库存', NULL),

-- 基础数据模块
('base:warehouse:view', '仓库查看', '基础数据', '查看仓库', NULL),
('base:warehouse:manage', '仓库管理', '基础数据', '管理仓库', NULL),
('base:location:view', '库位查看', '基础数据', '查看库位', NULL),
('base:location:manage', '库位管理', '基础数据', '管理库位', NULL),
('base:product:view', '商品查看', '基础数据', '查看商品', NULL),
('base:product:manage', '商品管理', '基础数据', '管理商品', NULL),
('base:supplier:view', '供应商查看', '基础数据', '查看供应商', NULL),
('base:supplier:manage', '供应商管理', '基础数据', '管理供应商', NULL);
```

---

### 2.4 角色权限关联表 (sys_role_permission)

```sql
CREATE TABLE sys_role_permission (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键ID',
    role_id         BIGINT NOT NULL COMMENT '角色ID',
    permission_id   BIGINT NOT NULL COMMENT '权限ID',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    UNIQUE KEY uk_role_permission (role_id, permission_id),
    INDEX idx_role_id (role_id),
    INDEX idx_permission_id (permission_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色权限关联表';
```

**预置角色权限**：

```sql
-- 超级管理员（拥有所有权限）
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT 1, id FROM sys_permission;

-- 仓库管理员（仓库业务全部权限）
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT 2, id FROM sys_permission WHERE module IN ('入库管理', '出库管理', '库存管理', '基础数据');

-- 入库员
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT 3, id FROM sys_permission WHERE module = '入库管理';

-- 出库员
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT 4, id FROM sys_permission WHERE module = '出库管理';

-- 库存管理员
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT 5, id FROM sys_permission WHERE module = '库存管理';

-- 报表查看员
INSERT INTO sys_role_permission (role_id, permission_id)
SELECT 6, id FROM sys_permission WHERE code LIKE '%view%' OR code LIKE '%report%';
```

---

### 2.5 仓库表 (sys_warehouse) - 基础数据

```sql
CREATE TABLE sys_warehouse (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '仓库ID',
    code            VARCHAR(20) NOT NULL UNIQUE COMMENT '仓库编码',
    name            VARCHAR(100) NOT NULL COMMENT '仓库名称',
    type            TINYINT NOT NULL COMMENT '类型：1-自营仓，2-第三方仓，3-海外仓',
    country         VARCHAR(50) COMMENT '国家',
    province        VARCHAR(100) COMMENT '省份/城市',
    address         VARCHAR(200) COMMENT '详细地址',
    manager         VARCHAR(50) COMMENT '负责人',
    phone           VARCHAR(20) COMMENT '联系电话',
    area            DECIMAL(10,2) COMMENT '总面积(㎡)',
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '状态：0-停用，1-启用',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_code (code),
    INDEX idx_status (status)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='仓库表';
```

**预设仓库数据**：

```sql
INSERT INTO sys_warehouse (code, name, type, country, province, status) VALUES
('WH-VN-001', '越南胡志明仓', 3, '越南', '胡志明市', 1),
('WH-ID-001', '印尼雅加达仓', 3, '印尼', '雅加达', 1),
('WH-CN-001', '深圳总仓', 1, '中国', '深圳', 1);
```

---

### 2.6 操作日志表 (sys_operation_log)

```sql
CREATE TABLE sys_operation_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
    time            DATETIME NOT NULL COMMENT '操作时间',
    user_id         BIGINT COMMENT '操作用户ID',
    username        VARCHAR(50) NOT NULL COMMENT '操作用户名',
    module          VARCHAR(50) COMMENT '操作模块',
    action          VARCHAR(50) COMMENT '操作类型',
    object          VARCHAR(200) COMMENT '操作对象',
    ip              VARCHAR(50) COMMENT 'IP地址',
    result          VARCHAR(20) COMMENT '操作结果：成功/失败',
    details         JSON COMMENT '详细数据',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_time (time),
    INDEX idx_user_id (user_id),
    INDEX idx_username (username),
    INDEX idx_module (module),
    INDEX idx_action (action),
    INDEX idx_result (result),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';
```

---

### 2.7 登录日志表 (sys_login_log)

```sql
CREATE TABLE sys_login_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
    time            DATETIME NOT NULL COMMENT '登录时间',
    user_id         BIGINT COMMENT '用户ID',
    username        VARCHAR(50) NOT NULL COMMENT '用户名',
    action          VARCHAR(20) NOT NULL COMMENT '动作：登录/登出',
    ip              VARCHAR(50) COMMENT 'IP地址',
    device          VARCHAR(100) COMMENT '登录设备',
    browser         VARCHAR(100) COMMENT '浏览器',
    result          VARCHAR(20) NOT NULL COMMENT '结果：成功/失败',
    fail_reason     VARCHAR(200) COMMENT '失败原因',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_time (time),
    INDEX idx_user_id (user_id),
    INDEX idx_username (username),
    INDEX idx_result (result),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='登录日志表';
```

---

### 2.8 库存变动日志表 (sys_inventory_log)

```sql
CREATE TABLE sys_inventory_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '日志ID',
    time            DATETIME NOT NULL COMMENT '变动时间',
    user_id         BIGINT COMMENT '操作用户ID',
    username        VARCHAR(50) NOT NULL COMMENT '操作用户名',
    sku_code        VARCHAR(50) NOT NULL COMMENT 'SKU编码',
    sku_name        VARCHAR(200) COMMENT 'SKU名称',
    warehouse_id    BIGINT COMMENT '仓库ID',
    warehouse_code   VARCHAR(20) COMMENT '仓库编码',
    location_code   VARCHAR(20) COMMENT '库位编码',
    batch_no        VARCHAR(50) COMMENT '批次号',
    change_type     VARCHAR(20) NOT NULL COMMENT '变动类型：入库/出库/调拨/盘点调整',
    change_quantity INT NOT NULL COMMENT '变动数量（正负）',
    quantity_before INT NOT NULL COMMENT '变动前库存',
    quantity_after  INT NOT NULL COMMENT '变动后库存',
    reference_no    VARCHAR(50) COMMENT '关联单号',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_time (time),
    INDEX idx_user_id (user_id),
    INDEX idx_sku_code (sku_code),
    INDEX idx_warehouse_id (warehouse_id),
    INDEX idx_change_type (change_type),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='库存变动日志表';
```

---

### 2.9 系统配置表 (sys_config)

```sql
CREATE TABLE sys_config (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '配置ID',
    category        VARCHAR(50) NOT NULL COMMENT '配置分类',
    code            VARCHAR(100) NOT NULL UNIQUE COMMENT '配置编码',
    name            VARCHAR(100) NOT NULL COMMENT '配置名称',
    value           TEXT COMMENT '配置值',
    type            VARCHAR(20) NOT NULL COMMENT '类型：string/number/boolean/select',
    default_value   TEXT COMMENT '默认值',
    options         JSON COMMENT '可选值（select类型）',
    validation_rule VARCHAR(500) COMMENT '校验规则',
    description     VARCHAR(500) COMMENT '描述',
    is_system       TINYINT NOT NULL DEFAULT 0 COMMENT '是否系统内置：0-否，1-是',
    sort_order      INT DEFAULT 0 COMMENT '排序',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_category (category),
    INDEX idx_code (code),
    INDEX idx_type (type),
    INDEX idx_is_system (is_system)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';
```

**配置数据**：

```sql
-- 基础参数配置
INSERT INTO sys_config (category, code, name, value, type, default_value, description, is_system) VALUES
('基础参数', 'system.name', '系统名称', 'WMS仓库管理系统', 'string', 'WMS仓库管理系统', '系统显示名称', 1),
('基础参数', 'system.language', '默认语言', 'zh-CN', 'select', 'zh-CN', '系统默认语言', 1, '{"options":["zh-CN","en-US"]}'),
('基础参数', 'system.timezone', '时区', 'GMT+8', 'select', 'GMT+8', '系统时区', 1, '{"options":["GMT+8","GMT+7"]}'),
('基础参数', 'system.currency', '货币单位', 'CNY', 'select', 'CNY', '货币单位', 1, '{"options":["CNY","USD","VND","IDR"]}'),

-- 业务参数配置
('业务参数', 'business.safety_days', '默认安全库存天数', '30', 'number', '30', '安全库存预警天数', 1),
('业务参数', 'business.validity_warning_days', '效期预警天数', '30', 'number', '30', '商品效期提前预警天数', 1),
('业务参数', 'business.stagnation_days', '呆滞天数', '90', 'number', '90', '库存呆滞预警天数', 1),
('业务参数', 'business.pick_path_strategy', '拣货路径策略', 'S形路径', 'select', 'S形路径', '拣货路径优化策略', 1, '{"options":["S形路径","最近优先"]}'),
('业务参数', 'business.stocktake_auto_freeze', '盘点时自动冻结', 'true', 'boolean', 'true', '盘点期间是否自动冻结库位', 1),

-- 编码规则配置
('编码规则', 'code.inbound_prefix', '入库单前缀', 'RK', 'string', 'RK', '入库单号前缀', 1),
('编码规则', 'code.outbound_prefix', '出库单前缀', 'CK', 'string', 'CK', '出库单号前缀', 1),
('编码规则', 'code.stocktake_prefix', '盘点单前缀', 'PD', 'string', 'PD', '盘点单号前缀', 1),
('编码规则', 'code.serial_length', '流水号位数', '4', 'number', '4', '单号流水号位数', 1),

-- 会话管理配置
('会话管理', 'session.timeout_minutes', '会话超时时间', '30', 'number', '30', '会话超时时间（分钟）', 1),
('会话管理', 'session.token_expire_hours', 'Access Token有效期', '1', 'number', '1', 'Access Token有效期（小时）', 1),
('会话管理', 'session.refresh_token_days', 'Refresh Token有效期', '7', 'number', '7', 'Refresh Token有效期（天）', 1),
('会话管理', 'session.allow_multi_login', '允许同一账号多设备登录', 'true', 'boolean', 'true', '是否允许同一账号多设备同时在线', 1),

-- 安全配置
('安全配置', 'security.min_password_length', '密码最小长度', '6', 'number', '6', '密码最小长度', 1),
('安全配置', 'security.max_password_length', '密码最大长度', '20', 'number', '20', '密码最大长度', 1),
('安全配置', 'security.login_fail_lock_count', '登录失败锁定次数', '5', 'number', '5', '连续登录失败锁定次数', 1),
('安全配置', 'security.password_expire_days', '密码过期天数', '90', 'number', '90', '密码过期天数，0为不过期', 1),
('安全配置', 'security.log_retention_days', '日志保留天数', '180', 'number', '180', '操作日志保留天数', 1),

-- 作业配置
('作业配置', 'operation.wave_auto_create', '波次自动创建', 'false', 'boolean', 'false', '是否自动创建波次', 1),
('operation.wave_merge_strategy', '波次合并策略', 'time', 'select', 'time', '波次合并策略', 1, '{"options":[{"value":"time","label":"按时段"},{"value":"express","label":"按快递"},{"value":"area","label":"按区域"}]}'),
('作业配置', 'operation.default_pick_strategy', '默认拣货策略', 'pick', 'select', 'pick', '默认拣货策略', 1, '{"options":[{"value":"pick","label":"摘果式"},{"value":"batch","label":"播种式"},{"value":"hybrid","label":"边拣边分"}]}'),
('作业配置', 'operation.enable_package_recommend', '启用包材推荐', 'true', 'boolean', 'true', '打包作业是否显示包材推荐', 1),
('作业配置', 'operation.pick_timeout_minutes', '拣货超时提醒', '30', 'number', '30', '拣货任务超时提醒（分钟）', 1),
('作业配置', 'operation.stocktake_diff_threshold', '盘点差异阈值', '5', 'number', '5', '盘点差异自动审核阈值（百分比）', 1),

-- 通知配置
('通知配置', 'notification.email_enabled', '启用邮件通知', 'false', 'boolean', 'false', '是否启用邮件通知', 1),
('通知配置', 'notification.sms_enabled', '启用短信通知', 'false', 'boolean', 'false', '是否启用短信通知', 1),
('notification.stock_warning_receivers', '库存预警接收人', '', 'string', '', '库存预警通知接收人，多个用逗号分隔', 1);
```

---

### 2.10 配置历史表 (sys_config_history)

```sql
CREATE TABLE sys_config_history (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '历史ID',
    config_id       BIGINT NOT NULL COMMENT '配置ID',
    old_value       TEXT COMMENT '修改前值',
    new_value       TEXT COMMENT '修改后值',
    operator_id     BIGINT NOT NULL COMMENT '操作人ID',
    operator_name   VARCHAR(100) NOT NULL COMMENT '操作人姓名',
    reason          VARCHAR(500) COMMENT '修改原因',
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',

    INDEX idx_config_id (config_id),
    INDEX idx_operator_id (operator_id),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配置历史表';
```

---

## 三、索引设计

| 表名 | 索引名 | 字段 | 类型 | 说明 |
|------|--------|------|------|------|
| sys_user | idx_username | username | 普通 | 用户名查询 |
| sys_user | idx_role_id | role_id | 普通 | 按角色筛选 |
| sys_user | idx_warehouse_id | warehouse_id | 普通 | 按仓库筛选 |
| sys_user | idx_status | status | 普通 | 按状态筛选 |
| sys_user | idx_create_time | create_time | 普通 | 时间范围查询 |
| sys_role | idx_code | code | 普通 | 角色编码查询 |
| sys_role | idx_type | type | 普通 | 角色类型筛选 |
| sys_permission | idx_module | module | 普通 | 按模块筛选 |
| sys_operation_log | idx_time | time | 普通 | 时间范围查询 |
| sys_operation_log | idx_user_id | user_id | 普通 | 按用户筛选 |
| sys_operation_log | idx_module | module | 普通 | 按模块筛选 |
| sys_config | idx_category | category | 普通 | 按分类筛选 |
| sys_config | idx_code | code | 唯一 | 配置编码查询 |

---

## 四、外键关系

```sql
-- 用户表外键
ALTER TABLE sys_user ADD CONSTRAINT fk_user_role
    FOREIGN KEY (role_id) REFERENCES sys_role(id);
ALTER TABLE sys_user ADD CONSTRAINT fk_user_warehouse
    FOREIGN KEY (warehouse_id) REFERENCES sys_warehouse(id);

-- 角色权限关联表外键
ALTER TABLE sys_role_permission ADD CONSTRAINT fk_rp_role
    FOREIGN KEY (role_id) REFERENCES sys_role(id);
ALTER TABLE sys_role_permission ADD CONSTRAINT fk_rp_permission
    FOREIGN KEY (permission_id) REFERENCES sys_permission(id);

-- 配置历史表外键
ALTER TABLE sys_config_history ADD CONSTRAINT fk_ch_config
    FOREIGN KEY (config_id) REFERENCES sys_config(id);
```

---

## 五、DDL 完整脚本

```sql
-- ============================================
-- WMS系统管理模块 - 数据库初始化脚本
-- 数据库: wms_system
-- ============================================

CREATE DATABASE IF NOT EXISTS wms_system DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
USE wms_system;

-- 1. 仓库表（基础数据，其他模块也可用）
CREATE TABLE sys_warehouse (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    code            VARCHAR(20) NOT NULL UNIQUE,
    name            VARCHAR(100) NOT NULL,
    type            TINYINT NOT NULL COMMENT '1-自营仓 2-第三方仓 3-海外仓',
    country         VARCHAR(50),
    province        VARCHAR(100),
    address         VARCHAR(200),
    manager         VARCHAR(50),
    phone           VARCHAR(20),
    area            DECIMAL(10,2),
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '0-停用 1-启用',
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='仓库表';

-- 2. 角色表
CREATE TABLE sys_role (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    code            VARCHAR(50) NOT NULL UNIQUE,
    name            VARCHAR(100) NOT NULL,
    type            TINYINT NOT NULL DEFAULT 2 COMMENT '1-预置 2-自定义',
    description     VARCHAR(500),
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '0-禁用 1-启用',
    is_system       TINYINT NOT NULL DEFAULT 0 COMMENT '0-否 1-是',
    create_user     BIGINT,
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_user     BIGINT,
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色表';

-- 3. 权限表
CREATE TABLE sys_permission (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    code            VARCHAR(100) NOT NULL UNIQUE,
    name            VARCHAR(100) NOT NULL,
    module          VARCHAR(50) NOT NULL,
    description     VARCHAR(500),
    parent_id       BIGINT,
    sort_order      INT DEFAULT 0,
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='权限表';

-- 4. 用户表
CREATE TABLE sys_user (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    username        VARCHAR(50) NOT NULL UNIQUE,
    password        VARCHAR(255) NOT NULL,
    name            VARCHAR(100) NOT NULL,
    phone           VARCHAR(20),
    email           VARCHAR(100),
    role_id         BIGINT NOT NULL,
    warehouse_id    BIGINT,
    status          TINYINT NOT NULL DEFAULT 1 COMMENT '0-禁用 1-启用',
    last_login_time DATETIME,
    last_login_ip   VARCHAR(50),
    create_user     BIGINT,
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_user     BIGINT,
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 5. 角色权限关联表
CREATE TABLE sys_role_permission (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    role_id         BIGINT NOT NULL,
    permission_id   BIGINT NOT NULL,
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_role_permission (role_id, permission_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='角色权限关联表';

-- 6. 操作日志表
CREATE TABLE sys_operation_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    time            DATETIME NOT NULL,
    user_id         BIGINT,
    username        VARCHAR(50) NOT NULL,
    module          VARCHAR(50),
    action          VARCHAR(50),
    object          VARCHAR(200),
    ip              VARCHAR(50),
    result          VARCHAR(20),
    details         JSON,
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='操作日志表';

-- 7. 登录日志表
CREATE TABLE sys_login_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    time            DATETIME NOT NULL,
    user_id         BIGINT,
    username        VARCHAR(50) NOT NULL,
    action          VARCHAR(20) NOT NULL,
    ip              VARCHAR(50),
    device          VARCHAR(100),
    browser         VARCHAR(100),
    result          VARCHAR(20) NOT NULL,
    fail_reason     VARCHAR(200),
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='登录日志表';

-- 8. 库存变动日志表
CREATE TABLE sys_inventory_log (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    time            DATETIME NOT NULL,
    user_id         BIGINT,
    username        VARCHAR(50) NOT NULL,
    sku_code        VARCHAR(50) NOT NULL,
    sku_name        VARCHAR(200),
    warehouse_id    BIGINT,
    warehouse_code  VARCHAR(20),
    location_code   VARCHAR(20),
    batch_no        VARCHAR(50),
    change_type     VARCHAR(20) NOT NULL,
    change_quantity INT NOT NULL,
    quantity_before INT NOT NULL,
    quantity_after  INT NOT NULL,
    reference_no    VARCHAR(50),
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='库存变动日志表';

-- 9. 系统配置表
CREATE TABLE sys_config (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    category        VARCHAR(50) NOT NULL,
    code            VARCHAR(100) NOT NULL UNIQUE,
    name            VARCHAR(100) NOT NULL,
    value           TEXT,
    type            VARCHAR(20) NOT NULL,
    default_value   TEXT,
    options         JSON,
    validation_rule VARCHAR(500),
    description     VARCHAR(500),
    is_system       TINYINT NOT NULL DEFAULT 0,
    sort_order      INT DEFAULT 0,
    create_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time     DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='系统配置表';

-- 10. 配置历史表
CREATE TABLE sys_config_history (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    config_id       BIGINT NOT NULL,
    old_value       TEXT,
    new_value       TEXT,
    operator_id     BIGINT NOT NULL,
    operator_name   VARCHAR(100) NOT NULL,
    reason          VARCHAR(500),
    created_at      DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='配置历史表';

-- 添加外键约束
ALTER TABLE sys_user ADD CONSTRAINT fk_user_role FOREIGN KEY (role_id) REFERENCES sys_role(id);
ALTER TABLE sys_user ADD CONSTRAINT fk_user_warehouse FOREIGN KEY (warehouse_id) REFERENCES sys_warehouse(id);
ALTER TABLE sys_role_permission ADD CONSTRAINT fk_rp_role FOREIGN KEY (role_id) REFERENCES sys_role(id);
ALTER TABLE sys_role_permission ADD CONSTRAINT fk_rp_permission FOREIGN KEY (permission_id) REFERENCES sys_permission(id);
ALTER TABLE sys_config_history ADD CONSTRAINT fk_ch_config FOREIGN KEY (config_id) REFERENCES sys_config(id);
```

---

*文档完成于 2026-04-11*
